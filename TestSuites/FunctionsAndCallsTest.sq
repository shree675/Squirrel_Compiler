int @sum(int a, int b){
    int c = a+b;
    return c; 
}

int @abc(){
    return 1;
}

int @start(){

    char a;
    int b;
    ``a = (int)((float)b + a * sum(a+b*(a+b), b+a*(b+a), a*b*b));``
    a = a+b;

    ``return a + sum(a,b);``
    return 0;
}
