void @start(){
    switch(var1){
        case (20.5): switch(var2){
                        case (50): int n;
                        default: int a;
                    }
        case (true): int j;
                     int q;
        default: int k;
    }
}