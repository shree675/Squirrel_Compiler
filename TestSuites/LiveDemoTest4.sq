``preprocessor directive``
replace init 141.8

void @start() {

    int n;
    input(n);           ``taking input from the user``

    float sum = init;

    for(int i=0; i<n; i=i+1){
        sum = sum + i
    }

    ``nested loops and selection
        statements are supported``

    if(sum>2*init){
        output(sum);
    }
    elif(sum==init){
        output(init);
    }
    else{
        float temp = sum + 1;
        output(temp);
    }

}
