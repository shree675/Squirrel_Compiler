void @start() {
    float arr[4] = { 0.0, 0.0, 0.0, 0.0 };
    float sum=1.0;
    for(int i = 0; i < 4; i=i+1) {
        arr[i]=(float) (i+1);
        sum = sum * arr[i];
        output(arr[i]);
        output("\n");
    }
    output(sum);
}